<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#f5f5f0] flex justify-center items-center min-h-screen">
  <div class="bg-cyan-800 p-4 rounded-xl w-80 shadow-lg">
    <!-- Top search/profile -->
    <div class="flex justify-between items-center bg-white rounded p-2 mb-4">
      <input class="outline-none w-full" placeholder="Search..." />
      <img src="/user-icon.png" alt="Profile" class="w-8 h-8 rounded-full ml-2" />
    </div>

    <!-- Temperature section -->
      <!-- Temperature range display -->
    <div class="bg-gradient-to-r from-yellow-200 to-yellow-300 rounded p-4 mb-4 border">
      <h2 class="text-lg font-semibold mb-3"><%=tempHeader %></h2>
      <div class="flex items-center justify-between text-sm font-medium mb-2">
        <span><%= minTemp %>°</span>
        <span><%= maxTemp %>°</span>
      </div>
      <div class="h-1.5 bg-white rounded-full w-full"></div>
    </div>

    <!-- Conditions + Rainfall section -->
    <div class="bg-gray-100 rounded border p-4 grid grid-cols-2 gap-2">
      <!-- Conditions -->
      <div>
        <h3 class="text-sm font-semibold border-b pb-1 mb-2"><%=conditionsHeader %></h3>
        <p class="text-lg"><%= conditions %></p>
        <div class="mt-6 border-t pt-2">
          <p class="text-m"><%= sunrise %></p>
          <div class="h-1 bg-gray-300 rounded-full my-1"></div>
          <p class="text-m"><%= sunset %></p>
        </div>
      </div>

      <!-- Rainfall -->
      <div class="flex flex-col justify-between items-center">
        <h3 class="text-sm font-semibold border-b pb-1 mb-2 w-full text-center">Rainfall</h3>
        <svg class="w-12 h-12 text-gray-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M16 13v2m-4-2v2m-4-2v2m10-6a4 4 0 10-8 0h-.5a4.5 4.5 0 000 9h13a4.5 4.5 0 000-9H18z" />
        </svg>
        <p class="text-lg"><%= rainfall %></p>
      </div>
    </div>
  </div>
</body>

</html>

