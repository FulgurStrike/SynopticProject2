<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Trading App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- For icons -->
    <style>
        body { background-color: #e2e8f0;  }
        .main-container {
            background-color: #166534; 
            max-width: 400px; 
            margin: 2rem auto;
            border-radius: 0.5rem;
            padding: 1.5rem; 
            box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
        }
        .scrollable-listings {
            max-height: 450px;
            overflow-y: auto;
            padding-right: 8px;
        }

        .scrollable-listings::-webkit-scrollbar {
            width: 8px;
        }
        .scrollable-listings::-webkit-scrollbar-track {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
        }
        .scrollable-listings::-webkit-scrollbar-thumb {
            background-color: rgba(255, 255, 255, 0.4);
            border-radius: 4px;
        }
        .scrollable-listings::-webkit-scrollbar-thumb:hover {
            background-color: rgba(255, 255, 255, 0.6);
        }
        .scrollable-listings {
            scrollbar-width: thin;
            scrollbar-color: rgba(255, 255, 255, 0.4) rgba(255, 255, 255, 0.1);
        }
    </style>
</head>
<body class="font-sans">
    <div class="main-container">
        <div class="bg-white p-3 rounded-md mb-4 flex items-center justify-between">
            <input type="text" placeholder="Search..." class="flex-grow border-none focus:ring-0 text-sm">
            <div class="flex items-center">
                <img src="/cat-call.gif" alt="User Avatar" class="w-8 h-8 rounded-full mr-2">
                <i class="fas fa-chevron-down text-gray-500"></i>
            </div>
        </div>
        <a href="/listings/new"
   class="block w-full bg-lime-300 hover:bg-lime-400 text-black font-semibold py-3 px-4 rounded-lg mb-6 text-center shadow-md transition duration-150 ease-in-out">
    <i class="fas fa-plus mr-2"></i> Create a Listing
</a>

<div class="space-y-4 scrollable-listings">
    <% if (listings.length > 0) { %>
        <% listings.forEach(listing => { %>
            <div class="bg-white p-4 rounded-lg shadow">
                <div class="flex">
                    <div style="background-color: '#cccccc'"
                         class="w-20 h-20 rounded-md mr-4 flex-shrink-0">
                    </div>
                    <div class="text-sm text-gray-700">
                        <p><strong>Item:</strong> <span class="border-b border-gray-400 inline-block min-w-[100px]"><%= listing.item %></span></p>
                        <p class="mt-1"><strong>Type:</strong> <span class="border-b border-gray-400 inline-block min-w-[80px]"><%= listing.type %></span></p>
                        <p class="mt-1"><strong>Quantity:</strong> <span class="border-b border-gray-400 inline-block min-w-[50px]"><%= listing.quantity %></span></p>
                        <p class="mt-1"><strong>Extra Details:</strong></p>
                        <p class="border-b border-gray-400 inline-block w-full min-h-[20px]"><%= listing.extraDetails %></p>
                    </div>
                </div>
            </div>
        <% }) %>
    <% } else { %>
        <p class="text-center text-white">No listings yet. Create one!</p>
    <% } %>
</div>
    </div>
        </body>
            </html>