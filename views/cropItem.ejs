<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= crop.name %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/ad45827d02.js" crossorigin="anonymous"></script>
</head>

<body class="bg-[#f5f5f0] flex justify-center items-center min-h-screen">
    <div class="bg-cyan-800 p-4 rounded-xl w-96 shadow-lg">
        <div class="flex justify-between items-center bg-white rounded p-2">
            <input class="outline-none w-full" placeholder="Search..." />
            <img src="/user-icon.png" alt="Profile" class="w-8 h-8 rounded-full ml-2" />
        </div>

        <img src="/user-icon.png" alt="<%= crop.name %>" class="w-48 h-48 justify-self-center m-2.5 rounded">
        <div class="bg-[#fffaf4] rounded border border-[#4b372e] p-2 space-y-3 h-[400px] overflow-y-auto">
        <h1 class="justify-self-center text-2xl italic"><strong><%= crop.name %></strong></h1>

        <ul class="grid grid-cols-2">
            <li class=""><i class="fa-solid fa-mountain-sun m-1"></i>Cycle: <%= crop.cycle %></li>
            <li class=""><i class="fa-solid fa-sun-plant-wilt m-1"></i>Drought Tolerant: <%= crop.droughttolerant %></li>
            <li class=""><i class="fa-solid fa-glass-water m-1"></i>Watering: <%= crop.watering %></li>
            <li class=""><i class="fa-solid fa-sun m-1"></i>Sun: <%= crop.sun.join(", ") %></li>
            <li class=""><i class="fa-solid fa-layer-group m-1"></i>Soil: <%= crop.soil %></li>
            <li class=""><i class="fa-solid fa-rocket m-1"></i>Growth Rate: <%= crop.growthrate %></li>
            <li class=""><i class="fa-solid fa-leaf m-1"></i>Fruits In: <%= crop.fruitsin.join(", ") %></li>
        </ul>

        <h2 class="justify-self-center text-lg underline">Description:</h1>
        <p><%= crop.description %></p>
        </div>
    </div>
</body>

</html>