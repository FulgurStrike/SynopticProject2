<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Trading App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- For icons -->
    <style>
        body { background-color: #e2e8f0;  }
        .main-container {
            background-color: #166534; 
            max-width: 400px; 
            margin: 2rem auto;
            border-radius: 0.5rem;
            padding: 1.5rem;
            box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -2px rgba(0,0,0,0.05);
        }
    </style>
</head>
<body class="font-sans">
    <div class="main-container">
        <div class="bg-white p-3 rounded-md mb-4 flex items-center justify-between">
            <input type="text" placeholder="Search..." class="flex-grow border-none focus:ring-0 text-sm">
            <div class="flex items-center">
                <img src="<%= user && user.avatarUrl ? user.avatarUrl : 'https://via.placeholder.com/30' %>" alt="User Avatar" class="w-8 h-8 rounded-full mr-2">
                <i class="fas fa-chevron-down text-gray-500"></i>
            </div>
        </div>
<h1 class="text-2xl font-bold text-white mb-6 text-center">Create a New Listing</h1>

<% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
        <strong class="font-bold">Error!</strong>
        <span class="block sm:inline"><%= errorMessage %></span>
    </div>
<% } %>

<form action="/listings" method="POST" class="bg-white p-6 rounded-lg shadow-md space-y-4">
    <div>
        <label for="item" class="block text-sm font-medium text-gray-700">Item Name:</label>
        <input type="text" id="item" name="item" required
               class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
               value="<%= typeof formData !== 'undefined' ? formData.item : '' %>">
    </div>
    <div>
        <label for="type" class="block text-sm font-medium text-gray-700">Type:</label>
        <input type="text" id="type" name="type" required
               class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
               value="<%= typeof formData !== 'undefined' ? formData.type : '' %>">
    </div>
    <div>
        <label for="quantity" class="block text-sm font-medium text-gray-700">Quantity:</label>
        <input type="number" id="quantity" name="quantity" required min="1"
               class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
               value="<%= typeof formData !== 'undefined' ? formData.quantity : '1' %>">
    </div>
    <div>
        <label for="extraDetails" class="block text-sm font-medium text-gray-700">Extra Details:</label>
        <textarea id="extraDetails" name="extraDetails" rows="3"
                  class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm"
        ><%= typeof formData !== 'undefined' ? formData.extraDetails : '' %></textarea>
    </div>
    <div>
        <button type="submit"
                class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
            Create Listing
        </button>
    </div>
    <div class="text-center mt-4">
        <a href="/listings" class="text-indigo-600 hover:text-indigo-800">Cancel</a>
    </div>
</form>

</div> 
</body>
</html>