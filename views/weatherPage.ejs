<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      overflow-x: hidden;
    }
  </style>
  <link rel="stylesheet" href="./CSS/Dropdown.css">
  <script src="./Scripts/Dropdown.js" defer></script>
  
</head>

<body class="bg-sky-100 min-h-screen flex items-center justify-center px-4 py-6">
  <div class="w-full max-w-sm sm:max-w-md bg-white rounded-xl shadow-lg border border-gray-300 p-4 min-h-[80vh] flex flex-col justify-between">

    <!-- Header -->
      <header class="mb-8 p-4 bg-white rounded-lg shadow-md flex items-center justify-end">
            <div class="relative">
                <div class="flex items-center space-x-2 cursor-pointer">
                    <div class="p-2 rounded-md bg-gray-200 hover:bg-gray-300 transition-colors duration-200">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-600" viewBox="0 0 20 20" fill="currentColor" id="profileMenuTrigger">
                            <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                        </svg>
                    </div>
                </div>
            </div>
        </header>

    <!-- Temperature -->
    <div class="bg-gradient-to-r from-yellow-200 to-yellow-300 rounded-lg p-4 border text-gray-800 mt-4">
      <h3 class="text-base font-bold mb-3">Temperature</h3>
      <div class="flex justify-between text-sm font-medium mb-2">
        <span><%= minTempLabel %><br><%= minTemp %>°</span>
        <span><%= maxTempLabel %><br><%= maxTemp %>°</span>
      </div>
      <div class="h-1.5 bg-white rounded-full w-full"></div>
    </div>

    <!-- Conditions + Rainfall -->
    <div class="bg-gray-50 rounded-lg p-4 border grid grid-cols-2 gap-4 text-sm mt-4">
      <!-- Conditions -->
      <div>
        <h4 class="font-semibold mb-1"><%= conditionsHeader %></h4>
        <p class="mb-2"><%= conditions %></p>
        <p class="text-xs text-gray-700"><strong>Sunrise:</strong> <%= sunrise %></p>
        <p class="text-xs text-gray-700"><strong>Sunset:</strong> <%= sunset %></p>
      </div>

      <!-- Rainfall -->
      <div class="flex flex-col items-center justify-center text-center">
        <h4 class="font-semibold mb-2">Rainfall</h4>
        <svg class="w-10 h-10 text-gray-600 mb-1" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
          <path d="M16 13v2m-4-2v2m-4-2v2m10-6a4 4 0 10-8 0h-.5a4.5 4.5 0 000 9h13a4.5 4.5 0 000-9H18z" />
        </svg>
        <p class="text-base font-medium"><%= rainfall %></p>
      </div>
    </div>

    <!-- Back Button -->
    <div class="pt-4">
      <a href="/farming" class="block">
        <button class="w-full bg-sky-500 hover:bg-sky-600 text-white py-2 rounded-lg text-sm font-semibold transition-colors duration-150">
          Back to Farming
        </button>
      </a>
    </div>

  </div>

   <div id="fullscreenMenu">
        <button id="closeMenuButton" aria-label="Close menu">
            &times; </button>
        <nav class="flex flex-col items-center">
            <a href="/" class="menu-item">Back to home</a>
            <a href="/AboutUs" class="menu-item">About Us</a>
        <% if (!isLoggedIn) { %>
            <a href="/login" class="menu-item">Login</a>
            <a href="/signUp" class="menu-item">Sign-up</a>
        <% } else { %>
            <a href="/logout" class="menu-item text-red-300 hover:bg-red-700 hover:text-red-100">Logout</a>
        <% } %>
        </nav>
    </div>
</body>

</html>



