<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= crop.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/ad45827d02.js" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/CSS/Dropdown.css">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            overflow-x: hidden;
        }
    </style>
</head>

<body class="bg-sky-100 min-h-screen flex flex-col items-center py-6 sm:py-10 px-4">
    <div class="w-full max-w-2xl mx-auto">
        <!-- Header -->
        <header class="mb-8 p-4 bg-white rounded-lg shadow-md flex items-center justify-between">
            <a href="/cropPage" class="inline-block">
                <button type="button" class="bg-sky-500 hover:bg-sky-600 text-white font-semibold py-2 px-4 rounded-lg text-sm transition-colors duration-150">
                    Back to Crops
                </button>
            </a>
            
            <div class="relative">
                <div class="flex items-center space-x-2 cursor-pointer"> 
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-600" viewBox="0 0 20 20" fill="currentColor" id="profileMenuTrigger">
                        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                </div>
            </div>
        </header>

        <!-- Main Content -->
        <main class="bg-white rounded-xl shadow-lg border-2 border-black p-6 space-y-6">
            <!-- Crop Image -->
            <div class="flex justify-center">
                <img src="<%= crop.imageurl %>" alt="<%= crop.name %>" class="w-48 h-48 rounded-xl shadow-md object-cover">
            </div>

            <!-- Crop Details Card -->
            <div class="bg-[#fffaf4] rounded-xl border border-[#4b372e] p-6 space-y-6">
                <h1 class="text-center text-3xl font-bold text-[#3a2c24] mb-4">
                    <%= crop.name %>
                </h1>

                <!-- Stats Grid -->
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 text-[#3a2c24]">
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-mountain-sun text-blue-600 w-5"></i>
                        <span><strong>Cycle:</strong> <%= crop.cycle %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-sun-plant-wilt text-orange-600 w-5"></i>
                        <span><strong>Drought Tolerant:</strong> <%= crop.droughttolerant %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-glass-water text-blue-500 w-5"></i>
                        <span><strong>Watering:</strong> <%= crop.watering %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-sun text-yellow-500 w-5"></i>
                        <span><strong>Sun:</strong> <%= crop.sun.join(", ") %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-layer-group text-amber-700 w-5"></i>
                        <span><strong>Soil:</strong> <%= crop.soil %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm">
                        <i class="fa-solid fa-rocket text-green-600 w-5"></i>
                        <span><strong>Growth Rate:</strong> <%= crop.growthrate %></span>
                    </div>
                    <div class="flex items-center space-x-2 p-3 bg-white rounded-lg shadow-sm sm:col-span-2">
                        <i class="fa-solid fa-leaf text-green-500 w-5"></i>
                        <span><strong>Fruits In:</strong> <%= crop.fruitsin.join(", ") %></span>
                    </div>
                </div>

                <!-- Description Section -->
                <div class="border-t border-[#4b372e] pt-4">
                    <h2 class="text-xl font-semibold text-[#3a2c24] mb-3 text-center">Description</h2>
                    <p class="text-[#3a2c24] leading-relaxed">
                        <%= crop.description %>
                    </p>
                </div>
            </div>
        </main>
    </div>

    <!-- Fullscreen Menu -->
    <div id="fullscreenMenu">
        <button id="closeMenuButton" aria-label="Close menu">
            &times;
        </button>
        <nav class="flex flex-col items-center">
            <a href="/" class="menu-item">Back to home</a>
            <a href="/login" class="menu-item">Login</a>
            <a href="/signUp" class="menu-item">Sign-up</a>
            <a href="#" class="menu-item text-red-300 hover:bg-red-700 hover:text-red-100">Logout</a>
        </nav>
    </div>

    <script src="/Scripts/Dropdown.js"></script>
</body>

</html>