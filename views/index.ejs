<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        /* You can add custom styles here if needed, but try to use Tailwind utility classes first */
        .hero-card {
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden; /* Ensures pseudo-element doesn't overflow */
        }
        .hero-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.3); /* Dark overlay for better text visibility */
            z-index: 1;
        }
        .hero-card > * {
            position: relative;
            z-index: 2; /* Ensure content is above the overlay */
        }
    </style>
</head>
<body class="bg-sky-100 min-h-screen flex flex-col items-center py-6 sm:py-10 px-4">

    <div class="w-full max-w-2xl mx-auto">
        <!-- Header -->
        <header class="mb-8 p-4 bg-white rounded-lg shadow-md flex items-center justify-end">
            <!-- Profile Icon Placeholder with dropdown arrow icon -->
            <div class="relative">
                <div class="flex items-center space-x-2 cursor-pointer"> 
                    <img src="user-icon.png" alt="User Profile" class="w-10 h-10 rounded-full border-2 border-sky-500">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-600" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                </div>
            </div>
        </header>

        <!-- Main Content Cards -->
        <main class="space-y-6">
            <!-- Farming Card -->
            <a href="/farming" class="block rounded-xl shadow-lg border-2 border-black hover:shadow-xl transition-shadow duration-300 hero-card text-white p-8 min-h-[200px] sm:min-h-[220px] flex flex-col justify-end" style="background-image: url('Farming3.png')">
                <h2 class="text-4xl sm:text-5xl font-bold mb-1">Farming</h2>
                <p class="text-lg sm:text-xl">Tips and Tricks for your farming needs</p>
            </a>

            <!-- Trading Card -->
            <a href="/trading" class="block rounded-xl shadow-lg border-2 border-black hover:shadow-xl transition-shadow duration-300 hero-card text-white p-8 min-h-[200px] sm:min-h-[220px] flex flex-col justify-end" style="background-image: url('Trading2.png');">
                <h2 class="text-4xl sm:text-5xl font-bold mb-1">Trading</h2>
                <p class="text-lg sm:text-xl">Trade Items with other vendors</p>
            </a>

            <!-- Market Card -->
            <a href="/market" class="block rounded-xl shadow-lg border-2 border-black hover:shadow-xl transition-shadow duration-300 hero-card text-white p-8 min-h-[200px] sm:min-h-[220px] flex flex-col justify-end" style="background-image: url('SwapShop2.png');">
                <h2 class="text-4xl sm:text-5xl font-bold mb-1">Market</h2>
                <p class="text-lg sm:text-xl">Stock information center</p>
            </a>
        </main>

        <!-- Footer Placeholder -->
        <footer class="mt-12 text-center text-gray-600 text-sm">
            <p>&copy; <script>document.write(new Date().getFullYear())</script> Crop helper. All rights reserved.</p>
        </footer>
    </div>

    <script>
        
    </script>
</body>
</html>
