<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Farming UI</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="./CSS/Dropdown.css">
</head>

<body class="bg-sky-100 min-h-screen flex flex-col items-center py-6 sm:py-10 px-4">
    <!-- Header -->
     <div class="w-full max-w-sm mx-auto">
        <header class="mb-8 p-4 bg-white rounded-lg shadow-md flex w-full items-center justify-end">
          <a href="/" class="inline-block">
                <button type="button" class="bg-sky-500 hover:bg-sky-600 text-white font-semibold py-2 px-4 rounded-lg text-sm mr-4 transition-colors duration-150 float-left">
                    Back to Homepage
                </button>
            </a>
            <div class="relative">
                <div class="flex items-center space-x-2 cursor-pointer"> 
                    
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-600" viewBox="0 0 20 20" fill="currentColor" id="profileMenuTrigger">
                        <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                </div>
            </div>
        </header>
        </div>
  <div class="bg-cyan-800 p-4 rounded-xl w-96 shadow-lg">
      <form action="/cropPage/search" method="post" class="flex justify-between items-center bg-white rounded p-2">
        <input name="search" class="outline-none w-full" placeholder="Search..." />
        <button type="submit" class="w-8 h-8 rounded-full ml-2">Go</button>
      </form>
    <div class="flex justify-center gap-[50px] mb-4 mt-4">
      <form action="/cropPage/sort" method="post">
        <div class="flex flex-col items-center">
          <select name="sort" class="p-2 rounded mb-3">
            <option>Sort By</option>
            <% sortOptions.forEach(option => { %>
            <option><%= option %></option>
            <% }) %>
          </select>
          <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Sort By</button>
        </div>
      </form>
      <form action="/cropPage/filter" method="post">
        <div class="flex flex-col items-center">
          <select name="filter" class="p-2 rounded mb-3">
            <option>Filter By</option>
            <% filterOptions.forEach(option => { %>
            <option><%= option %></option>
            <% }) %>
          </select>
          <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Filter By</button>      
        </div>
      </form>
    </div>
    <!-- Placeholder Cards -->
    <!-- Crop Cards Container -->
    <div class="bg-[#fffaf4] rounded border border-[#4b372e] p-2 space-y-3 h-[400px] overflow-y-auto">
      <% crops.forEach(crop=> { %>
        <a class="block" href="/cropItemPage/<%= crop._id %>">
        <div class="bg-[#d6bfa6] rounded p-3 flex items-center space-x-3">
          <div class="w-16 h-16 bg-[#8d735d] rounded flex items-center justify-center text-white font-bold text-xl">
            🌱
          </div>
          <div class="flex-1 space-y-1 text-sm text-[#3a2c24]">
            <div><strong>Name:</strong>
              <%= crop.name %>
            </div>
            <div><strong>Watering:</strong>
              <%= crop.watering %>
            </div>
            <div><strong>Cycle:</strong>
              <%= crop.cycle %>
            </div>
            <div><strong>Sun:</strong>
              <%= crop.sun.join(", ") %>
            </div>
            <div><strong>Drought Tolerant:</strong>
              <%= crop.droughttolerant %>
            </div>
          </div>
        </div>
        </a>
        <% }) %>
    </div>
  </div>
  <div id="fullscreenMenu">
        <button id="closeMenuButton" aria-label="Close menu">
            &times; </button>
        <nav class="flex flex-col items-center">
            <a href="/" class="menu-item">Back to home</a>
        <% if (!isLoggedIn) { %>
            <a href="/login" class="menu-item">Login</a>
            <a href="/signUp" class="menu-item">Sign-up</a>
        <% } else { %>
            <a href="/logout" class="menu-item text-red-300 hover:bg-red-700 hover:text-red-100">Logout</a>
        <% } %>
        </nav>
    </div>

    <script src="./Scripts/Dropdown.js"></script>
</body>
</html>
